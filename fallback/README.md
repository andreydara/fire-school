# Fallback assets

Cached inputs for running the core practicals when a live API, cloud backend, or venue internet connection fails.

Target contents will include compact Galičica subsets such as pre/post-fire imagery or derived rasters, fire-history tables, recovery time series, weather subsets, and susceptibility predictor layers.

Every core practical should eventually be able to switch from live data to these cached inputs without changing the analytical logic.
